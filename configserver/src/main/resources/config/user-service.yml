spring:
  data:
    mongodb:
      uri: ${DB_URL}
      database: ${DB_DATABASE}
  RabbitMQ:
    host: localhost
    port: 5672
    username: admin
    password: admin123
management:
  endpoints:
    web:
      exposure:
        include: "*"
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
    register-with-eureka: true
    fetch-registry: true
server:
  port: 8082