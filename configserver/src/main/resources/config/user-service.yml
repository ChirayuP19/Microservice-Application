spring:
  data:
    mongodb:
      uri: ${DB_URL}
      database: ${DB_DATABASE}
  RabbitMQ:
    host: localhost
    port: 5672
    username: admin
    password: admin123
management:
  endpoints:
    web:
      exposure:
        include: "*"
server:
  port: 8082